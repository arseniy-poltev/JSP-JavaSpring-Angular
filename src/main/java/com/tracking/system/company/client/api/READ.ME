The API is the main contract used and exposed in controllers.
Contains business logic and uses one or more services if needed.
Inputs and Outputs are DTOs Transforms DTOs to model objects for the services.